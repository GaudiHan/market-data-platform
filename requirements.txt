# Ingestion
websockets>=12.0
aiohttp>=3.9
python-dotenv>=1.0
orjson>=3.9

# Storage
asyncpg>=0.29        # Timescale/Postgres
pymongo>=4.6

# Backtesting / analysis
pandas>=2.1
numpy>=1.26

# Order book
sortedcontainers>=2.4  # O(log n) price-level inserts/removals, standard for L2 book reconstruction

# Testing
pytest>=8.0
pytest-asyncio>=0.23
mongomock>=4.1   # in-memory Mongo for testing repository logic without a real server
